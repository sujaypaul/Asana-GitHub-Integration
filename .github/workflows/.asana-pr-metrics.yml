name: Asana PR Metrics
on:
  pull_request:
    types: [opened, edited, labeled, unlabeled, review_requested, closed]
  schedule:
    - cron: '5 16 * * 2' # 0 0 * * 1
jobs:
  validate-asana-link:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action != 'closed' && github.event.action != 'review_requested'
    steps:
      - name: Check for Asana Task URL
        id: check-url
        run: |
          DESCRIPTION="${{ github.event.pull_request.body }}"
          if [[ "$DESCRIPTION" =~ "https://app.asana.com" ]] && ! [[ "${{ github.event.pull_request.labels.*.name }}" =~ "hotfix" ]]; then
            echo "Asana task URL found"
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "No valid Asana task URL or hotfix label detected"
            echo "status=failure" >> $GITHUB_OUTPUT
          fi
      - name: Create PR Attachment on Asana Task
        if: steps.check-url.outputs.status == 'success'
        uses: Asana/create-app-attachment-github-action@latest
        with:
          asana-secret: ${{ secrets.ASANA_SECRET }}
      - name: Fail if No Asana Task URL
        if: steps.check-url.outputs.status == 'failure' && !contains(github.event.pull_request.labels.*.name, 'hotfix')
        run: |
          echo "Error: No valid Asana task URL found in PR description"
          exit 1

  weekly-unlinked-pr-report:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - uses: actions/checkout@v3
      - name: Fetch PRs
        run: |
          curl -H "Authorization: Bearer ${{ github.token }}" \
               -H "Accept: application/vnd.github.v3+json" \
               https://api.github.com/repos/${{ github.repository }}/pulls?state=open > prs.json
      - name: Check Unlinked PRs
        run: |
          UNLINKED_COUNT=0
          UNLINKED_PRS=""
          while IFS= read -r pr; do
            PR_NUMBER=$(echo "$pr" | jq -r '.number')
            DESCRIPTION=$(echo "$pr" | jq -r '.body // ""')
            LABELS=$(echo "$pr" | jq -r '.labels[].name')
            if ! echo "$DESCRIPTION" | grep -q "https://app.asana.com" && ! echo "$LABELS" | grep -q "hotfix"; then
              UNLINKED_COUNT=$((UNLINKED_COUNT + 1))
              UNLINKED_PRS="$UNLINKED_PRS\n- PR #$PR_NUMBER: $(echo "$pr" | jq -r '.title')"
            fi
          done < <(jq -c '.[]' prs.json)
          echo "Unlinked PRs: $UNLINKED_COUNT" > unlinked_report.txt
          echo -e "Unlinked PRs:\n$UNLINKED_PRS" >> unlinked_report.txt
      - name: Create GitHub Issue
        uses: peter-evans/create-issue-from-file@v4
        with:
          title: "Weekly Unlinked PR Report"
          content-filepath: unlinked_report.txt
          labels: report, unlinked-prs

  track-review-to-merge:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && (github.event.action == 'review_requested' || github.event.action == 'closed')
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Record Review Requested Time
        if: github.event.action == 'review_requested'
        run: |
          mkdir -p .github/review-times
          echo "Review requested at $(date -u +%Y-%m-%dT%H:%M:%SZ)" > .github/review-times/review-time-${{ github.event.pull_request.number }}.txt
          echo "PR_NUMBER=${{ github.event.pull_request.number }}" >> .github/review-times/review-time-${{ github.event.pull_request.number }}.txt
          ls -la .github/review-times/ >> debug.txt # Debug: Verify file creation
      - name: Commit Review Time to Dedicated Branch
        if: github.event.action == 'review_requested'
        run: |
          git config --unset-all credential.helper || echo "No credential helper to unset"
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git config http.https://github.com/.extraheader "Authorization: basic $(echo -n x-access-token:${{ secrets.REPO_ACCESS_TOKEN }} | base64)" || echo "Failed to set Git credential header"
          git checkout -b review-times || git checkout review-times
          git add .github/review-times/review-time-${{ github.event.pull_request.number }}.txt || echo "Failed to add file"
          git commit -m "Store review time for PR #${{ github.event.pull_request.number }}" || echo "No changes to commit"
          git push origin review-times 2>&1 | tee push_output.txt
          echo "Push exit code: $?" >> push_output.txt
          cat push_output.txt >> debug.txt
          git status >> debug.txt
          git log -n 1 >> debug.txt
          git config --list >> debug.txt # Debug: Show Git config
        continue-on-error: true
      - name: Checkout Review Times Branch
        if: github.event.action == 'closed' && github.event.pull_request.merged
        uses: actions/checkout@v4
        with:
          ref: review-times
      - name: Calculate Review-to-Merge Time
        if: github.event.action == 'closed' && github.event.pull_request.merged
        id: calculate-time
        run: |
          REVIEW_TIME_FILE=".github/review-times/review-time-${{ github.event.pull_request.number }}.txt"
          if [ -f "$REVIEW_TIME_FILE" ]; then
            REVIEW_TIME=$(grep "Review requested at" "$REVIEW_TIME_FILE" | cut -d' ' -f4)
            MERGE_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
            REVIEW_EPOCH=$(date -d "$REVIEW_TIME" +%s)
            MERGE_EPOCH=$(date -d "$MERGE_TIME" +%s)
            TIME_DIFF=$((MERGE_EPOCH - REVIEW_EPOCH))
            HOURS=$((TIME_DIFF / 3600))
            MINUTES=$(( (TIME_DIFF % 3600) / 60 ))
            echo "Review-to-merge time: ${HOURS}h ${MINUTES}m" > metrics.txt
            echo "HOURS=$HOURS" >> $GITHUB_ENV
            echo "MINUTES=$MINUTES" >> $GITHUB_ENV
            echo "comment_text=PR #${{ github.event.pull_request.number }} merged. Review-to-merge time: ${HOURS}h ${MINUTES}m." >> $GITHUB_OUTPUT
          else
            echo "Review-to-merge time: Not available (no review time file)" > metrics.txt
            echo "HOURS=0" >> $GITHUB_ENV
            echo "MINUTES=0" >> $GITHUB_ENV
            echo "comment_text=PR #${{ github.event.pull_request.number }} merged. Review-to-merge time: Not available (no review time file)." >> $GITHUB_OUTPUT
          fi
      - name: Extract AI-Assisted Line Changes
        if: github.event.action == 'closed' && github.event.pull_request.merged
        run: |
          DESCRIPTION="${{ github.event.pull_request.body }}"
          if [[ "$DESCRIPTION" =~ \+([0-9]+)-([0-9]+) ]]; then
            ADDED_LINES="${BASH_REMATCH[1]}"
            REMOVED_LINES="${BASH_REMATCH[2]}"
            echo "AI-assisted lines: +${ADDED_LINES}-${REMOVED_LINES}" >> metrics.txt
            echo "ai_lines=+${ADDED_LINES}-${REMOVED_LINES}" >> $GITHUB_OUTPUT
          else
            echo "No AI-assisted line changes found" >> metrics.txt
            echo "ai_lines=none" >> $GITHUB_OUTPUT
          fi
        id: extract-ai-lines
      - name: Comment on Asana Task
        if: github.event.action == 'closed' && github.event.pull_request.merged
        uses: Asana/comment-on-task-github-action@v1.2
        with:
          asana-secret: ${{ secrets.ASANA_SECRET }}
          comment-text: "PR #${{ github.event.pull_request.number }} merged. Review-to-merge time: ${{ steps.calculate-time.outputs.comment_text }}. AI-assisted lines: ${{ steps.extract-ai-lines.outputs.ai_lines }}"
